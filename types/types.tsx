
export type Props = {
    children?: JSX.Element | undefined
}

export type PropsObject = {
    name: string,
    url: string
}

export type types = {
    slot: number,
    type: {[key: string]: string}
}